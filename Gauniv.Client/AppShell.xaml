<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Gauniv.Client.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:Gauniv.Client.Pages"
       xmlns:vm="clr-namespace:Gauniv.Client.ViewModel"
       xmlns:service="clr-namespace:Gauniv.Client.Services"
       Shell.FlyoutBehavior="Flyout"
       Shell.NavBarIsVisible="true"
       Title="Gauniv"
       BindingContext="{vm:MenuViewModel}"
       FlyoutBackgroundColor="{StaticResource BgMain}">

    <Shell.FlyoutHeader>
        <Grid HeightRequest="200" Background="{StaticResource BlackBrush}">
            <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                <Label Text="GAUNIV" 
                       FontSize="30" 
                       FontAttributes="Bold" 
                       TextColor="{StaticResource Primary}" 
                       CharacterSpacing="6"
                       HorizontalOptions="Center"/>
                <Label Text="{Binding UserName}" 
                       FontSize="16" 
                       TextColor="White" 
                       HorizontalOptions="Center"/>
                <Label Text="{Binding UserEmail}" 
                       FontSize="12" 
                       TextColor="{StaticResource TextMuted}" 
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Grid>
    </Shell.FlyoutHeader>

    <Shell.ItemTemplate>
        <DataTemplate>
            <Grid Padding="20,12" ColumnDefinitions="40,*">
                <Label Text="{Binding Icon}" 
                       FontSize="20" 
                       TextColor="White"
                       VerticalOptions="Center" 
                       HorizontalOptions="Center"/>
                <Label Grid.Column="1" 
                       Text="{Binding Title}" 
                       FontSize="15" 
                       TextColor="White" 
                       VerticalOptions="Center" 
                       Margin="10,0,0,0"/>
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>

    <FlyoutItem Title="Catalogue"
                Icon="🎮"
                Route="games">
        <ShellContent ContentTemplate="{DataTemplate local:Index}" />
    </FlyoutItem>

    <FlyoutItem Title="Ma Bibliothèque"
                  Icon="📚"
                  Route="mygames"
                  IsVisible="{Binding IsNotAdmin}">
        <ShellContent ContentTemplate="{DataTemplate local:MyGames}" />
    </FlyoutItem>

    <FlyoutItem Title="Gestion des jeux"
                  Icon="🛠️"
                  Route="admin"
                  IsVisible="{Binding IsAdmin}">
        <ShellContent ContentTemplate="{DataTemplate local:AdminPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Mes Amis"
                  Icon="👥"
                  Route="friends"
                  IsVisible="{Binding IsNotAdmin}">
        <ShellContent ContentTemplate="{DataTemplate local:MyFriends}" />
    </FlyoutItem>

    <FlyoutItem Title="Profil"
                  Icon="👤"
                  Route="profile">
        <ShellContent ContentTemplate="{DataTemplate local:Profile}" />
    </FlyoutItem>




</Shell>
