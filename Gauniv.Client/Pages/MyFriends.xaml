<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Gauniv.Client.ViewModel"
             xmlns:dto="clr-namespace:Gauniv.Client.Dtos"
             x:Class="Gauniv.Client.Pages.MyFriends"
             Title="Mes Amis"
             x:DataType="vm:MyFriendsViewModel"
             BackgroundColor="#1b2838">

    <Grid RowDefinitions="Auto,Auto,Auto,*">
        <!-- Header -->
        <Label Grid.Row="0" Text="Mes Amis" FontSize="32" FontAttributes="Bold" TextColor="#c7d5e0" Margin="20,20,20,10"/>
        
        <!-- Search Bar -->
        <Frame Grid.Row="1" BackgroundColor="#16202d" BorderColor="Transparent" CornerRadius="8" Padding="10" Margin="20,0">
            <Grid ColumnDefinitions="*,Auto">
                <Entry Grid.Column="0" Text="{Binding FriendSearchText}" Placeholder="Rechercher un ami..." PlaceholderColor="#8f98a0" TextColor="White" BackgroundColor="Transparent"/>
                <Button Grid.Column="1" Text="Ajouter" Command="{Binding AddFriendCommand}" BackgroundColor="#66c0f4" TextColor="White" CornerRadius="8"/>
            </Grid>
        </Frame>



        <ScrollView Grid.Row="3" Padding="20">
            <VerticalStackLayout Spacing="20">
                
                <!-- Pending Requests - EN HAUT (priorité) - MASQUÉ SI VIDE -->
                <VerticalStackLayout IsVisible="{Binding IncomingRequests.Count, Converter={StaticResource IntToBoolConverter}}">
                    <Label Text="Demandes Reçues" FontSize="18" TextColor="#66c0f4" FontAttributes="Bold" Margin="0,0,0,10"/>
                    <CollectionView ItemsSource="{Binding IncomingRequests}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="dto:FriendDto">
                                <Frame BackgroundColor="#2a475e" BorderColor="#66c0f4" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                    <Grid ColumnDefinitions="*,Auto,Auto">
                                        <Label Grid.Column="0" Text="{Binding UserName}" FontSize="18" TextColor="White" VerticalOptions="Center"/>
                                        <Button Grid.Column="1" Text="✔" BackgroundColor="#10b981" TextColor="White" Margin="0,0,10,0" WidthRequest="40"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MyFriendsViewModel}}, Path=AcceptRequestCommand}"
                                                CommandParameter="{Binding .}"/>
                                        <Button Grid.Column="2" Text="✖" BackgroundColor="#ff6b6b" TextColor="White" WidthRequest="40"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MyFriendsViewModel}}, Path=RemoveFriendCommand}"
                                                CommandParameter="{Binding .}"/>
                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>

                <!-- Outgoing Requests - MASQUÉ SI VIDE -->
                <VerticalStackLayout IsVisible="{Binding OutgoingRequests.Count, Converter={StaticResource IntToBoolConverter}}">
                    <Label Text="Demandes Envoyées" FontSize="18" TextColor="#8f98a0" FontAttributes="Bold" Margin="0,0,0,10"/>
                    <CollectionView ItemsSource="{Binding OutgoingRequests}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="dto:FriendDto">
                                <Frame BackgroundColor="#16202d" BorderColor="#2a475e" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <Label Grid.Column="0" Text="{Binding UserName}" FontSize="18" TextColor="#8f98a0" VerticalOptions="Center"/>
                                        <Button Grid.Column="1" Text="Annuler" BackgroundColor="Transparent" TextColor="#8f98a0"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MyFriendsViewModel}}, Path=RemoveFriendCommand}"
                                                CommandParameter="{Binding .}"/>
                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
                
                <!-- MY FRIENDS -->
                <VerticalStackLayout>
                    <Label Text="Mes Amis" FontSize="20" TextColor="White" FontAttributes="Bold" Margin="0,0,0,10"/>
                    <CollectionView ItemsSource="{Binding Friends}">
                        <CollectionView.EmptyView>
                            <Label Text="Veuillez vous connecter pour accéder à votre liste d'amis" TextColor="#8f98a0" HorizontalOptions="Center" Margin="20"/>
                        </CollectionView.EmptyView>
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="dto:FriendDto">
                                <Frame BackgroundColor="#16202d" BorderColor="#2a475e" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <VerticalStackLayout Grid.Column="0">
                                            <Label Text="{Binding UserName}" FontSize="18" FontAttributes="Bold" TextColor="White"/>
                                            <HorizontalStackLayout Spacing="5">
                                                <Label Text="🟢" IsVisible="{Binding IsOnline}" FontSize="12"/>
                                                <Label Text="{Binding IsOnline, Converter={StaticResource BoolToStringConverter}, ConverterParameter='En ligne|Hors ligne'}" TextColor="#8f98a0" FontSize="12"/>
                                            </HorizontalStackLayout>
                                        </VerticalStackLayout>
                                        <Button Grid.Column="1" Text="X" TextColor="#ff6b6b" BackgroundColor="Transparent"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MyFriendsViewModel}}, Path=RemoveFriendCommand}"
                                                CommandParameter="{Binding .}"/>
                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
                
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
